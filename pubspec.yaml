name: gcode_core
description: Pure Dart G-code parsing, line reading, and toolpath building core.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
